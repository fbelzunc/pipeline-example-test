metadata:
  labels:
    pipeline: jenkinsfile
    tier: A
spec:
  containers:
  - name: "maven"
    image: "maven"
    imagePullPolicy: "IfNotPresent"
    resources:
      limits: {}
      requests: {}
    securityContext:
      privileged: false
