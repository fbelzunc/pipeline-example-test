metadata:
  labels:
    pipeline: jenkinsfile
    tier: A
spec:
  containers:
  - name: "jnlp"
    image: "cloudbees/cloudbees-core-agent:latest"
    imagePullPolicy: "IfNotPresent"
    resources:
      limits:
        memory: "512Mi"
      requests: {}
    securityContext:
      privileged: false
