metadata:
  labels:
    pipeline: jenkinsfile
    tier: A
spec:
  containers:
  - name: "maven"
    image: "maven"
    imagePullPolicy: "IfNotPresent"
    resources:
      memory: "4096Mi"
      cpu: "0.5"
    securityContext:
      privileged: false
